requests==2.32.3
# Pinned <2, not the latest — this GDAL base image's own osgeo.gdal_array
# C extension is compiled against NumPy 1.x's ABI; installing NumPy 2.x
# imports fine on its own but crashes the moment GDAL's ReadAsArray() (or
# anything else touching gdal_array) actually runs ("ImportError: numpy...
# failed to import" / "compiled using NumPy 1.x cannot be run in NumPy
# 2.1.3"), live-verified 2026-08-05 running this container for real.
numpy==1.26.4
Pillow==11.0.0
# Ocean-currents source (US4) — free CMEMS account required, see
# fetch_currents.py's module docstring. Unpinned: this is a fast-moving
# package (frequent releases tracking Copernicus Marine's own API), and
# nothing else in this container shares a transitive dependency with it.
copernicusmarine
# copernicusmarine pulls in h5netcdf (via xarray) for writing the NetCDF
# result, but not h5netcdf's own h5py dependency — live-verified 2026-08-05:
# without this, subset() fails at the final to_netcdf() write step with
# "No module named 'h5py'".
h5py
# Air-quality overlay source (spec 054 US2) — free Copernicus Atmosphere
# Data Store (ADS) account required, see fetch_overlay_cams.py's module
# docstring. Unpinned, same rationale as copernicusmarine above.
cdsapi
